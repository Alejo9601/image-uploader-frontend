import { MotionBar } from "../styled/MotionBar";
import { StaticBar } from "../styled/StaticBar";

const ProgressBar = () => {
  return (
    <StaticBar>
      <MotionBar />
    </StaticBar>
  );
};

export default ProgressBar;
